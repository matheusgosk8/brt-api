# Local (docker compose na raiz btr/)
DATABASE_URL=postgresql://postgres:docker@localhost:5433/brt-be

# Prod (Neon) — NÃO commitarr; use pooled na Lambda, direto no migrate/seed
# DATABASE_URL=postgresql://USER:PASSWORD@ep-XXXX-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require

PORT=3001
CORS_ORIGIN=*
LOG_LEVEL=INFO
DB_POOL_MAX=2
